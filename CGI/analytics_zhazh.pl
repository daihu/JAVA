#!/usr/bin/perl
#author:paul;
use warnings;
use diagnostics;
use Carp;
open (FH,"<localhost_access_log.2012-02-08.txt") or die "Couldn't open  for reading:$!";
while ($line=<FH>){
	 
	}
#close($line);