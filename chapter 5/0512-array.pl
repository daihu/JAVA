#!/usr/bin/perl
use warnings;
use diagnostics;

@name=("Guy", "Tom", "Dan",  "Roy");
@list=(2..10);
@grades=(100, 90, 65, 96, 40, 75);
@items=($a, $b, $c);
@empty=();
$size=@items;
@mammals = qw/dogs cats cows/;
@fruit = qw(apples pears peaches);
