# .bashrc

# User specific aliases and functions

alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi
alias ds='cd /zhazh/files/systemfiles'
alias db='cd /usr/apache-tomcat-6.0.29/webapps/BizServ/WEB-INF'
~                                                                  